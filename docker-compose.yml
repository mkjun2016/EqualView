services:
  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      context: ./backend
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      REDIS_URL: redis://redis:6379/0
      UPLOAD_DIR: /app/uploads
      HF_HOME: /root/.cache/huggingface
    volumes:
      - ./backend/uploads:/app/uploads
      - whisper_cache:/root/.cache/huggingface
    depends_on:
      redis:
        condition: service_healthy

  worker:
    build:
      context: ./backend
    command: celery -A celery_app worker --loglevel=info --concurrency=${CELERY_WORKER_CONCURRENCY:-3}
    env_file:
      - ./backend/.env
    environment:
      REDIS_URL: redis://redis:6379/0
      UPLOAD_DIR: /app/uploads
      HF_HOME: /root/.cache/huggingface
    volumes:
      - ./backend/uploads:/app/uploads
      - whisper_cache:/root/.cache/huggingface
    depends_on:
      redis:
        condition: service_healthy

volumes:
  whisper_cache:
